/**
 * 工具函数索引文件 - 统一导出所有工具函数
 */
import { getAvatarUrl } from './avatar'
import request from './request'
import socketService from './socket'

export {
  getAvatarUrl,
  request,
  socketService
}

export default {
  getAvatarUrl,
  request,
  socket
}